<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test.ticket.tickettools.dao.TaskDetailDao">
    <resultMap id="BaseResultMap" type="test.ticket.tickettools.domain.entity.TaskDetailEntity">
        <constructor>
            <arg column="id" javaType="java.lang.Long" jdbcType="BIGINT"/>
            <arg column="task_id" javaType="java.lang.Long" jdbcType="BIGINT"/>
            <arg column="user_name" javaType="java.lang.String" jdbcType="VARCHAR"/>
            <arg column="ID_card" javaType="java.lang.String" jdbcType="VARCHAR"/>
            <arg column="user_phone" javaType="java.lang.String" jdbcType="VARCHAR"/>
            <arg column="age" javaType="java.lang.Integer" jdbcType="INTEGER"/>
            <arg column="create_date" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
            <arg column="update_date" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
            <arg column="done" javaType="java.lang.Boolean" jdbcType="BIT"/>
            <arg column="payment" javaType="java.lang.Boolean" jdbcType="BIT"/>
        </constructor>
    </resultMap>
    <insert id="insert" parameterType="test.ticket.tickettools.domain.entity.TaskDetailEntity">
        insert into task_detail (id, task_id, user_name,
                                 ID_card, user_phone, age,
                                 create_date, update_date, done, payment)
        values (#{id,jdbcType=BIGINT}, #{taskId,jdbcType=BIGINT}, #{userName,jdbcType=VARCHAR},
                #{idCard,jdbcType=VARCHAR}, #{userPhone,jdbcType=VARCHAR}, #{age,jdbcType=INTEGER},
                #{createDate,jdbcType=TIMESTAMP}, #{updateDate,jdbcType=TIMESTAMP}, #{done,jdbcType=BIT}, #{payment,jdbcType=BIT})
    </insert>
    <insert id="insertBatch" parameterType="java.util.List">
        insert into task_detail (task_id, user_name,
        ID_card, user_phone, age,
        create_date, update_date)
        VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.taskId,jdbcType=BIGINT}, #{item.userName,jdbcType=VARCHAR},
            #{item.IDCard,jdbcType=VARCHAR}, #{item.userPhone,jdbcType=VARCHAR}, #{item.age,jdbcType=INTEGER},
            #{item.createDate,jdbcType=TIMESTAMP}, #{item.updateDate,jdbcType=TIMESTAMP}
            )
        </foreach>
    </insert>
    <update id="updateTaskDetailBath" parameterType="java.util.List">
        <foreach collection="list" item="entity" separator=";">
            UPDATE task_detail
            SET done = #{entity.done}
            WHERE id = #{entity.id}
        </foreach>
    </update>
    <select id="selectByTaskId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select *
        from task_detail
        where task_id = ${id}
    </select>
    <select id="selectByTaskIdLimit" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select *
        from task_detail
        where task_id = ${id} and done!=0 limit 5
    </select>
    <update id="updateTaskDetail" parameterType="test.ticket.tickettools.domain.entity.TaskDetailEntity">
        update task_detail
        <set>
            <if test='payment!=null'>
                payment = #{payment},
            </if>
            <if test='updateDate!=null'>
                update_date = #{updateDate}
            </if>
        </set>
        where task_id = #{taskId}
    </update>
</mapper>
